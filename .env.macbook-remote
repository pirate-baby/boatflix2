# Remote Transcoding Configuration for Your MacBook Pro
# Add these to your .env file on the Raspberry Pi

# Enable remote transcoding
REMOTE_TRANSCODE_ENABLED=true

# MacBook connection details
# Use IP address (more reliable) or hostname
REMOTE_TRANSCODE_HOST=192.168.50.111
# Alternative: REMOTE_TRANSCODE_HOST=Pirate-Baby-Pro.local

# SSH username on the MacBook
REMOTE_TRANSCODE_USER=ethan

# Path to SSH private key (inside container)
# This assumes you mounted the key at /app/data/.ssh/id_pi_transcode
REMOTE_TRANSCODE_SSH_KEY=/app/data/.ssh/id_pi_transcode

# SSH port (default is 22)
REMOTE_TRANSCODE_PORT=22

# Working directory on MacBook for temporary files
# This will be created automatically
REMOTE_TRANSCODE_WORK_DIR=/tmp/boatflix-transcode

# ==========================================
# Don't forget to also add to docker-compose.yml:
#
# services:
#   manager:
#     volumes:
#       - ./data:/app/data
#       - ~/boatflix-data/.ssh/id_pi_transcode:/app/data/.ssh/id_pi_transcode:ro
#
# Or copy the key to your data directory:
#   mkdir -p ./data/.ssh
#   # Copy the key from MACBOOK_SETUP_COMPLETE.md
#   chmod 600 ./data/.ssh/id_pi_transcode
